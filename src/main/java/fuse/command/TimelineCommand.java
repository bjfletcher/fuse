package fuse.command;

/**
 * A timing command.
 */
public class TimelineCommand extends Command {

  /**
   * Creates a timeline command.
   * @param userName the timeline's username.
   */
  public TimelineCommand(final String userName) {
    super(userName);
  }

}
