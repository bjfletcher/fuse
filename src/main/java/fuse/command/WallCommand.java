package fuse.command;

/**
 * A wall command.
 */
public class WallCommand extends Command {

  /**
   * Creates a wall command.
   * @param userName the wall's username.
   */
  public WallCommand(final String userName) {
    super(userName);
  }

}
